{
  "compilerOptions": {
    "module": "esnext",
    "target": "es2018",
    "jsx": "react-jsx",
    "lib": ["dom", "es2015", "es2017"],
    "strict": true,
    "noEmit": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "removeComments": false,
    "preserveConstEnums": true,
    "allowSyntheticDefaultImports": true,
    // `node` (i.e. node10) was removed in TypeScript 7. `bundler` is the right
    // replacement here — Vite does the bundling — and it is compatible with
    // the `"module": "esnext"` above.
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "types": ["vitest/globals"],
  },
  "include": ["src", "vite.config.ts", "vite.ladle.config.ts", "setupTests.ts"],
  "exclude": ["node_modules", "dist", "docs"]
}
